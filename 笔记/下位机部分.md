# 下位机部分

## 一、遥控器

### 1.概况：使用天地飞et08

​	<u>使用时channel5通道映射可能会和其他通道相反，但是不要修改，飞机还要用</u>

相关网址：[航模遥控器,遥控器,发射机,发射端,-天地飞-深圳市天地飞科技开发有限公司](http://www.wflysz.com/product/)

### 2.S-BUS

1.SBUS是一个接收机串行总线输出，通过这根总线，可以获得遥控器上所有通道的数据，也是一种串口通信协议，100000波特率，偶校验，8bits数据位，2bits停止位。下图是et10

![image-20260408172531264](C:\Users\13608\AppData\Roaming\Typora\typora-user-images\image-20260408172531264.png)

2.s-bus串口中只打开RX即可，数据位为8位但是Worldlength字长需要设为9位

相关网址：1.短时长S-BUS讲解：【航模遥控器SBUS协议代码讲解！】https://www.bilibili.com/video/BV11F411W7bZ?vd_source=4af39ba9cb9157c85950214d95b779a2

## 二、stm32h7

## 1.区别

​	与F1,F4相比，H7主要区别在于多了一个L1 Cache一级缓存，在数据一致性上存在新的问题，外设性能的提升针对ADC,DMA,USART,USB多了一些新配置，同时只支持HAL库，不支持标准库了，从编程上来看基本没有差别。

## 2.stm32h7系列总线系统

![image-20260409181832014](C:\Users\13608\AppData\Roaming\Typora\typora-user-images\image-20260409181832014.png)

三个作用于之间有互联，D1-to-D2 AHB,D1-to-D3 AHB,D2-to-D1 AHB,D2-to-D3 AHB,而且各作用域分到的SRAM不同，一般数据存在D2 domain

## 3.重要数据类型，变量和堆栈

### (1)局部变量

只在本函数内有效，不同函数中可以使用相同名字的变量，形式参数也是局部变量。如果在同一个源文件中，外部变量和局部变量同名，则在局部变量的作用范围内，外部变量不起作用。可以在别的文件中使用extern引用

### (2)全局变量

在文件中被所有函数公用，但是移植文件时需要将所有引用的外部变量及其值一起移植

### (3)static关键字

​	有时候希望函数中的局部变量的值在函数调用结束后不消失而保留原值，即占用的存储单元不释放，在下一次该函数调用时，该变量已有值，就是上一次函数调用结束时的值。这时可以使用关键字 static 进行声明。

​	用 static 声明一个变量的作用： 
◆ 对局部变量用 static 声明，则使用该变量在整个程序执行期间不释放，为其分配的的空间始终存在。 
◆ 全局变量用 static 声明，则该变量的作用域只限于本文件模块（即被声明的文件中）。

## 4.HAL库

